= simple_form_for :session, url: session_path do |f|
  = f.input :email
  = f.input :password
  .form-actions
    = f.button :submit, t('helpers.submit.session.submit')
    or
    = link_to t(".forgot_password"), new_password_path
    instead
