version: '2'

options:
  verbose: true
syncs:
  carmen-sync:
    src: '.'
    # sync_strategy: 'native_osx' # not needed, this is the default now
    sync_excludes: ['.gitignore', '.byebug_history', '.git', 'tmp', 'README.md', '.docker*', '.idea']
